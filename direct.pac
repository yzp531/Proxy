/* ============================================================
 *  PAC 自动代理配置 —— 全部直连（不使用任何代理）
 *  地址：https://proxy.i-xx.top/direct.pac
 * ------------------------------------------------------------
 *  所有请求一律 DIRECT，等价于"关闭代理"但保留 PAC 配置项，
 *  用于快速在代理与直连之间切换。
 * ============================================================ */

function FindProxyForURL(url, host) {
    return "DIRECT";
}
